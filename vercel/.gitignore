node_modules
.next
out
.env
.env.local
.env.*.local
*.tsbuildinfo
.vercel
.data/

# Generated by `npm run mirror` (auto-run on predev / prebuild). These are
# real dirs with file symlinks back to ../Frontend/ — they're rebuilt from
# Frontend/ on every dev/build, so they should NOT be committed.
/app/
/components/
/lib/
/public/
/middleware.ts
/postcss.config.mjs
/tailwind.config.ts
